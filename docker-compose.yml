version: '3'
services:
  app:
    build:
      context: .
      dockerfile: rails.Dockerfile
    volumes:
      - .:/app:cached
    ports:
      - 3000:3000
    environment:
      - DB_HOST=db # osidoridevdb.cbukuyrfnctk.ap-northeast-1.rds.amazonaws.com
      - DB_PORT=3306
      - DB_NAME=dev_osdr_db
      - DB_USER=devusr
      - DB_PWD=pass

    # production
    #command:  bundle exec rake db:create RAILS_ENV=production
    #command:  bundle exec rake db:migrate RAILS_ENV=production
    #command:  bundle exec rake db:seed RAILS_ENV=production
    #command:  bundle exec puma -t 5:5 -p 3000 -e production -C config/puma/production.rb

    # development
    command:  bundle exec rails s -p 3000 -b 0.0.0.0

    # command:  bundle exec puma -p 3000 -e development -C config/puma/development.rb
    # command:  bundle exec puma -t 5:5 -b 0.0.0.0 -p 3000 -e production -C config/puma/production.rb -d
    # command:  bundle exec puma -p 3000

    depends_on: 
      - db
  db:
    build:
      context: .
      dockerfile: mysql.Dockerfile
    environment:
      MYSQL_ROOT_PASSWORD: pwd
      MYSQL_USER: devusr
      MYSQL_PASSWORD: passtoken_expires_at
      MYSQL_DATABASE: dev_osdr_db
    ports:
      - 3307:3306
    volumes:
      - ./tmp/db/mysql_data:/var/lib/mysql # データの永続化
      - ./docker/db/sql:/docker-entrypoint-initdb.d # 初期データ投入
